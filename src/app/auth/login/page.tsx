import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login",
  description: "Login to access your account and create or join rooms",
};

import LoginForm from "@/components/pages/Auth/LoginForm";

export default LoginForm;
